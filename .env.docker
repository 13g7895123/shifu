# Docker environment variables
PORT=3001
NODE_ENV=production

# Backend API Configuration
# 後端 API 的完整路徑（包含協議、主機和埠號）
# 此設定會被前端用來呼叫後端 API
BACKEND_API_URL=http://localhost:3001

# Redis configuration
REDIS_URL=redis://redis:6379
REDIS_PASSWORD=passwordshifu

# Frontend configuration
FRONTEND_URL=http://localhost:80
DOMAIN_NAME=localhost:3001

# Application settings
APP_NAME=luckygo
APP_VERSION=1.0.0

# Security Configuration (change these in production!)
SESSION_SECRET=3629d513857f8808cc722b0b7ea2677b19c44cce3a781108826de80605aeb3cb
JWT_SECRET=868d04f1baf666729c5eb0b6b543d43a3b0b224e145f7dd9a0c8097d1bb423f1

# Repository Configuration
# Options: 'memory' or 'sqlite'
REPOSITORY_TYPE=sqlite

# SSL/TLS (for production with nginx)
# SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
# SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# Database configuration (for future database integration)
# DATABASE_URL=postgresql://username:password@postgres:5432/luckygo
# DATABASE_HOST=postgres
# DATABASE_PORT=5432
# DATABASE_NAME=luckygo
# DATABASE_USER=luckygo_user
# DATABASE_PASSWORD=secure_db_password
